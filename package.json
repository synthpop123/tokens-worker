{
  "name": "tokens-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted Cloudflare Worker backend for the tokens CLI (usage reporting to D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.111.0"
  }
}
